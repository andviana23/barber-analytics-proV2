{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug API",
      "type": "go",
      "request": "launch",
      "program": "${workspaceFolder}/cmd/api",
      "mode": "auto",
      "env": {
        "DATABASE_URL": "postgres://..."
      }
    }
  ]
}
